Drop your preferred headshot here named exactly:  james-headshot.jpg
Recommended: square (e.g. 800x800), <300KB.

Until you add it, the site automatically falls back to your existing
TheSmokies.com author photo (see the <img onerror> handler in index.html),
so the page never shows a broken image.
